# Historical mobile prototype

This directory is retained for reference only. The active mobile app is the
native SwiftUI app in `../ios`.

Use the commands in the repository root README for current iOS development.
